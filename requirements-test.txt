# Integration test environment: Home Assistant 2026.8 requires Python >=3.14.2.
homeassistant==2026.8.3
pytest-homeassistant-custom-component==0.13.357
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-aiohttp==1.1.1
pytest-timeout==2.4.0

# Install the Bridge package and its test dependencies in the same environment.
# Its suite has a separate pytest lifecycle because the Home Assistant plugin's
# global leak checks are intentionally stricter than the legacy worker tests.
-e ./bridge_service[test]
